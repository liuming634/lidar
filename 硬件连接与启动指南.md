# 硬件连接与启动指南

## 一、项目现状说明

**相机驱动已完成，雷达驱动可直接连接硬件。** 当前项目已具备完整硬件驱动能力，可以直接启动相机和雷达进行测试。

| 硬件 | 能否直接用 | 说明 |
|------|-----------|------|
| Livox Avia 雷达 | 能连上，但结果没意义 | 加载的是 RM2025 的 PCD 地图，在新场地定位会失败，动态检测也会乱 |
| 海康相机 | ✅ 能直接用 | 独立包 `hik_camera` 已完成，`ros2 run hik_camera hik_camera_node` 启动 |
| rosbag 测试 | ✅ 能完整跑通 | 数据和配置匹配，适合先熟悉整个流程 |

### 外部依赖说明

启动前确认以下 ROS 包已安装：

```bash
# foxglove_bridge（可视化调试用，非必须但 launch 文件中引用了）
sudo apt install ros-humble-foxglove-bridge

# 如果缺少，启动时需要在 radar.launch.py 中注释掉对应行（见 5.1）
```

> `databag_tool::BagRecorderNode` 项目里没有实现，也找不到系统安装包，**启动前必须在 launch 文件中注释掉**，否则容器加载会失败。

---

## 二、激光雷达（Livox Avia）

### 2.1 修改广播码

每个 Livox 雷达机身贴纸上有唯一的广播码。将驱动中所有 `cmdline_bd_code` 和 `broadcast_code` 改为雷达的广播码。

修改 `src/livox_driver/livox_ros2_driver/launch/livox_lidar_launch.py` 第 16 行：

```python
cmdline_bd_code = 'livox你的广播码'   # 改为雷达机身上的广播码
```

同样修改 `src/livox_driver/livox_ros2_driver/config/livox_lidar_config.json` 第 4 行的广播码，并确认 `enable_connect` 为 `true`：

```json
"broadcast_code": "livox你的广播码",
"enable_connect": true,
```

> 两个文件中的广播码必须一致，否则雷达连接不上。驱动中其他 launch 文件（`livox_template_launch.py`、`livox_lidar_msg_launch.py` 等）也建议一并修改。

### 2.2 接线与网络

雷达用网线直连电脑（或通过交换机）。

你的有线网卡名是 `enp3s0`，推荐以下设置：

```
电脑 IP：192.168.1.100（静态）
子网掩码：255.255.255.0
网关：192.168.1.1
```

设置方法（桌面版 Ubuntu — **设置 → 网络 → 有线 → 齿轮图标 → IPv4 → 手动**，填入上面三个值；或用命令 `nmcli con mod enp3s0 ipv4.addresses 192.168.1.100/24 ipv4.method manual`）。

> 网线插上后用 `ip a` 确认 `enp3s0` 状态从 `NO-CARRIER` 变为 `UP`，且有配置的 IP 地址。

### 2.3 启动雷达

```bash
# 先 source 环境
source install/setup.bash
ros2 launch livox_ros2_driver livox_lidar_launch.py
```

连接成功会看到以下输出（关键信息）：

```
[livox_ros2_driver_node-1] Livox-SDK init success!
[livox_ros2_driver_node-1] Lidar[3GGDJA600100601] status_code[0] working state[1] feature[0]
[livox_ros2_driver_node-1] Lidar start sample success
```

> `Lidar start sample success` 出现即表示雷达正常工作、正在发送点云数据。

也可用 rviz 可视化查看（注意 Fixed Frame 手动输入 `livox_frame`）：

```bash
rviz2
# Fixed Frame 下拉框手动输入 livox_frame 回车
# Add → PointCloud2 → Topic 选 /livox/lidar
```
 
> 雷达驱动只发点云数据不发 TF，所以 rviz2 的 Fixed Frame 里没有 `livox_frame` 可选。直接打字输入即可。

或在终端确认有数据：

```bash
ros2 topic echo /livox/lidar --once
```

---

## 三、相机驱动（海康相机，已完成）

海康相机 ROS2 节点已完成，独立包 `src/hik_camera/`。

### 3.1 SDK 已安装情况

你的系统里已经安装了海康 SDK：

| 内容 | 路径 |
|------|------|
| SDK 安装目录（完整） | `/opt/MVS/` |
| 核心头文件 | `/opt/MVS/include/MvCameraControl.h` |
| 核心库文件 | `/opt/MVS/lib/64/libMvCameraControl.so` |
| 官方可视化工具 | `/opt/MVS/bin/MVS`（先开这个确认相机能出图） |
| 示例代码 | `/opt/MVS/Samples/` |

原始安装包（可删除，已用不上）：
```
/home/lm/Ubuntu/code/rm/MVS_Linux_STD_V5.0.0_260421/    ← 757MB
/home/lm/Ubuntu/code/rm/MVS_Linux_STD_V5.0.0_260421.zip  ← 757MB
```
> SDK 装在 `/opt/MVS/`，所以你的项目里不需要再单独拷贝 SDK 库文件，CMakeLists.txt 直接引用系统路径即可。

### 3.2 SDK 开发文档位置

SDK 自带了完整的开发文档（无需去官网找）：

- **C 语言开发指南（英文）**：`/opt/MVS/doc/Machine Vision Camera SDK Developer Guide Linux (C) V4.7.0/`
- **C 语言开发指南（中文）**：`/opt/MVS/doc/工业相机Linux SDK开发指南V4.7.0（C）/`
- **Python 开发指南**：`/opt/MVS/doc/工业相机Linux SDK开发指南V4.7.0（Python）/`

用浏览器打开对应的 `.html` 文件即可查看所有 API 说明。

### 3.3 SDK 核心 API 调用流程

节点中实际调用的 API 顺序如下（详见 `hik_camera_node.cpp`）：

| 步骤 | SDK 函数 | 说明 |
|------|---------|------|
| 1. 枚举设备 | `MV_CC_EnumDevices(MV_USB_DEVICE, &list)` | 查找连接到电脑的相机 |
| 2. 创建句柄 | `MV_CC_CreateHandle(&handle, pDeviceInfo)` | 创建相机操作句柄 |
| 3. 打开设备 | `MV_CC_OpenDevice(handle)` | 与相机建立连接 |
| 4. 设置参数 | `MV_CC_SetFloatValue(handle, name, val)` | 曝光时间、增益等 |
| 5. 设置参数 | `MV_CC_SetEnumValue(handle, name, val)` | 曝光模式、白平衡等 |
| 6. 设置帧率 | `MV_CC_SetFrameRate(handle, fps)` | 限制最大帧率 |
| 7. 开始取流 | `MV_CC_StartGrabbing(handle)` | 相机开始发送图像数据 |
| 8. 获取图像 | `MV_CC_GetImageBuffer(handle, &frame, timeout)` | 拿到一帧图像（轮询） |
| 9. 释放缓冲区 | `MV_CC_FreeImageBuffer(handle, &frame)` | 用完释放（必须配对调用） |
| 10. 停止取流 | `MV_CC_StopGrabbing(handle)` | 暂停图像采集 |
| 11. 关闭设备 | `MV_CC_CloseDevice(handle)` | 断开连接 |
| 12. 销毁句柄 | `MV_CC_DestroyHandle(handle)` | 清理资源 |

> USB 相机枚举时参数传 `MV_USB_DEVICE`；如果是网口相机则传 `MV_GIGE_DEVICE`。

另需注意图像格式转换：
- 海康输出的原始格式通常是 Bayer（如 `PixelType_Gvsp_BayerRG8`）
- 需要用 OpenCV 的 `cv::cvtColor()` 转为 BGR/RGB（参考 `sp_vision_25` 封装的转换映射表）

### 3.4 已有封装可供参考（备选）

另一个项目 `sp_vision_25` 中有更完整的海康相机封装（含自动重连、参数动态配置等），后续需要增强功能时可参考：

```
/home/lm/Ubuntu/code/rm/sp_vision_25-main/io/hikrobot/
├── hikrobot.hpp        ← 头文件（类定义）
├── hikrobot.cpp        ← 实现文件（完整 API 调用流程）
├── include/
│   └── MvCameraControl.h
└── lib/amd64/
    └── libMvCameraControl.so
```

这个封装包含了：
- 相机枚举、创建、打开、参数设置、取图、关闭的完整流程
- 自动 USB 重连机制（掉线后自动 reset USB 重新连接）
- Bayer 格式到 BGR 的转换
- 曝光时间、增益等参数设置接口

你可以直接参考它的调用顺序来写 ROS2 节点。

### 3.5 项目目录结构（已完成）

相机节点是独立包，放在工作空间根 `src/` 下（与 `tdt_vision/` 平级）：

```
src/hik_camera/                      ← 独立 ROS2 包
├── src/
│   └── hik_camera_node.cpp          ← 节点代码（已完成）
├── CMakeLists.txt
└── package.xml
```

> 这是标准的 ROS2 独立包结构，不依赖 `tdt_vision/` 里的任何代码，单独编译、单独运行。
> SDK 在 `/opt/MVS/` 系统级安装，不放项目里。

#### 部署到小电脑时需要什么

| 内容 | 要不要放进项目 (git) | 说明 |
|------|---------------------|------|
| `src/hik_camera/` 整个目录 | ✅ 放项目里 | 节点源码 + 编译配置 |
| `/opt/MVS/`（SDK） | ❌ 不放项目 | 每台机器各自装到 `/opt/MVS/` |

> 小电脑上没有 SDK 的话，去海康官网下 MVS Linux 版安装，或者直接把开发机的 `/opt/MVS/` 整个拷过去。

### 3.6 CMakeLists.txt（已完成）

直接引用系统 SDK 路径，详见实际文件 `src/hik_camera/CMakeLists.txt`。

### 3.7 节点代码（见 `src/hik_camera/src/hik_camera_node.cpp`）

已实现的功能：
- 枚举 USB 相机，自动连接第一个
- Bayer → BGR 格式转换
- 发布 `/camera_image` 话题（`sensor_msgs/Image`）
- 节点退出时自动释放 SDK 资源

> 参考 `sp_vision_25` 的封装可以进一步完善自动 USB 重连、参数动态配置等。

### 3.8 操作步骤

```bash
# 1. 先用官方工具测试相机（确认硬件正常）
/opt/MVS/bin/MVS

# 2. 编译（首次或改代码后）
cd ~/Ubuntu/code/rm/T-DT_Radar
source /opt/ros/humble/setup.bash
colcon build --symlink-install

# 3. 启动相机节点
source install/setup.bash
ros2 run hik_camera hik_camera_node

# 4. 另一个终端查看图像
source install/setup.bash
ros2 run rqt_image_view rqt_image_view
# 在窗口中选择 /camera_image 话题
```

> 相机节点独立运行，不依赖 `tdt_vision` 管线。其他节点只要订阅 `/camera_image` 就能收到数据。

### 3.9 关于雷达管线中的相机节点

`tdt_vision` 的 `radar.launch.py` 里原本引用了一个 `tdt_vision::NodeCamera` 组件插件。当前相机节点是独立运行的，没有注册到这个 launch 文件里。如果以后想集成进去，需要改为 rclcpp_components 组件模式，并在 `tdt_vision/CMakeLists.txt` 中注册。

目前保持独立 `ros2 run` 启动即可，不影响功能。

---

## 四、编译与构建

首次使用或修改源码后需要重新编译：

```bash
cd ~/Ubuntu/code/rm/T-DT_Radar

# 先 source ROS 环境
source /opt/ros/humble/setup.bash

# 删除旧的编译产物（如需清理）
rm -rf build/ install/ log/

# 编译（使用项目提供的脚本）
./build.sh
# 或手动编译
# colcon build --symlink-install
```

---

## 五、启动流程

### 5.1 rosbag 测试（推荐）

先通过 rosbag 把整个流程跑通，熟悉代码。

> ⚠️ **准备工作：** rosbag 数据在 `rosbag/全明星赛第一局bag.zip` 中，需要先解压：
> ```bash
> cd rosbag
> unzip 全明星赛第一局bag.zip
> ```
> 解压后得到 `rosbag/` 目录（内含 `rosbag.db3` 等文件），记下这个目录的**绝对路径**。

> ⚠️ **修改 launch 文件：** `src/tdt_vision/launch/radar.launch.py` 中包含 3 个当前无法加载的节点，启动前先注释掉：
>
> ```python
> # 第87行：相机驱动（未实现）
> # hik_camera_node = get_camera_node('tdt_vision', 'tdt_vision::NodeCamera')
>
> # 第91行：foxglove（如未安装系统包）
> # foxglove_node = get_foxglove_node('foxglove_bridge', 'foxglove_bridge::FoxgloveBridge')
>
> # 第92行：录包工具（不存在）
> # record_node = get_record_node('databag_tool', 'BagRecorderNode')
>
> # 第94行：从容器参数列表中删掉对应的变量名
> cam_detector = get_camera_detector_container(
>     radar_detect_node,
>     radar_resolve_node,
>     tdt_debug_node,
> )
> ```

```bash
# 终端1：启动雷达识别管线
ros2 launch dynamic_cloud lidar.launch.py

# 终端2：启动视觉识别管线
ros2 launch tdt_vision radar.launch.py

# 终端3：播放 rosbag 数据（用解压后的实际路径替换）
ros2 bag play /path/to/rosbag/rosbag
```

### 5.2 真实雷达测试（可连硬件，但配准会失败）

```bash
# 终端1：Livox 雷达驱动
ros2 launch livox_ros2_driver livox_lidar_launch.py

# 终端2：雷达识别管线
ros2 launch dynamic_cloud lidar.launch.py
```

此时各话题有数据但结果不正确，因为 PCD 地图和新场景不匹配。

### 5.3 一键启动（使用 tmux）

项目根目录下已提供 `startup.sh` 脚本，用 tmux 在一个终端窗口中同时启动多个节点。

先安装 tmux：
```bash
sudo apt install tmux
```

使用一键启动：
```bash
chmod +x startup.sh
./startup.sh
```

tmux 快捷键说明：
- `Ctrl+B` + 数字键 — 在各个窗口间切换（driver=0, lidar=1）
- `Ctrl+B` + `d` — 断开但不退出（后台继续运行）
- `tmux attach -t radar` — 重新连接

> 如果不想用 tmux，也可以在 `lidar.launch.py` 里用 `IncludeLaunchDescription` 把 Livox 驱动包含进来，一个 `ros2 launch` 同时启动两者。

### 5.4 完整融合运行（相机 + 雷达 + 管线）

```bash
# 终端1：Livox 雷达驱动
ros2 launch livox_ros2_driver livox_lidar_launch.py

# 终端2：相机驱动
ros2 run hik_camera hik_camera_node

# 终端3：视觉识别管线（需先注释 radar.launch.py 中不可用的节点，见 5.1）
ros2 launch tdt_vision radar.launch.py

# 终端4：雷达识别管线
ros2 launch dynamic_cloud lidar.launch.py

# 终端5（可选）：可视化
rviz2
# 或
ros2 run rqt_image_view rqt_image_view
```

---

## 六、首次部署必须做的初始化

### 6.1 录制静态场景地图

清场后启动雷达，确保场地内没有运动物体：

```bash
# 终端1：启动雷达
ros2 launch livox_ros2_driver livox_lidar_launch.py

# 终端2：录制点云（10~20秒后 Ctrl+C 停止）
ros2 bag record /livox/lidar -o ./rosbag/field_map
```

### 6.2 提取 PCD 文件

从 rosbag 中提取点云保存为 PCD，放到 `config/field_map.pcd`，然后修改以下文件中的 PCD 路径：

| 文件 | 修改位置 | 内容 |
|------|---------|------|
| `src/lidar/dynamic_cloud/src/dynamic_cloud.cpp` | 第 21 行 | `loadPCDFile("config/RM2025.pcd"` → `config/field_map.pcd` |
| `src/lidar/localization/src/localization.cpp` | 第 33 行 | `target_pcd_file = "config/RM2025.pcd"` → `config/field_map.pcd` |

> 两个 .cpp 文件中的 PCD 路径都是相对于 `ros2 launch` 执行时的工作目录，建议使用绝对路径或确认启动目录与 `config/` 的相对关系。

### 6.3 相机外参标定

现有的 `calibrate` 节点使用的是 RM 赛场的固定建筑坐标（五点标定），换场景后需要改用标定板重新标定，产出 `config/out_matrix.yaml`。

标定命令（一起启动相机 + 标定节点）：

```bash
# 先 source 环境
source install/setup.bash

# 启动相机 + 标定节点
ros2 launch tdt_vision calibrate_radar.launch.py
```

操作步骤：
1. 窗口出现后，将相机对准场地
2. 按 **Enter** 进入标定模式
3. 在图像上依次用 **鼠标左键** 点击 5 个场地特征点（我方基地 → 我方前哨站 → 敌方基地 → 敌方前哨站 → 敌方高地）
4. 选点后用 **WASD** 微调像素位置，按 **n** 确认
5. 选完 5 个点自动调用 solvePnP 解算，结果写入 `config/out_matrix.yaml`

5 个特征点的 3D 坐标在 `config/radar_config.yaml` 的 `calibrate_points` 中配置。

---

### 6.4 修改配置文件

## 七、代码修改清单

完成硬件适配需要修改的部分：

| 文件 | 修改内容 | 优先级 |
|------|---------|-------|
| `livox_lidar_launch.py` | 改广播码（第16行） | 必须 |
| `livox_lidar_config.json` | 改 broadcast_code + enable_connect 设为 true | 必须 |
| 其他 livox launch 文件（`template/lidar_msg/lidar_rviz/hub_*`） | 改广播码，建议统一修改 | 建议 |
| `dynamic_cloud.cpp` | 替换 PCD 路径（第21行）、改场地过滤条件 | 必须 |
| `localization.cpp` | 替换 PCD 路径（第33行） | 必须 |
| `radar.launch.py` | 注释掉 camera_node / foxglove_node / record_node | 必须 |
| `calibrate` 相关 | 废弃旧标定，改用标定板方案 | 必须 |
| `radar_utils.cpp` + `RM2025_Points.yaml` | 区域多边形（平地可删掉） | 按需 |
| `resolve.cpp` / `kalman_filter.cpp` / `debug_map.cpp` | 15/28 场地尺寸硬编码 | 按需 |
| `src/hik_camera/` | ✅ 已完成，无需修改 | 已完成 |

---

## 八、常见问题

| 现象 | 可能原因 |
|------|---------|
| Livox 连不上 | 广播码没改，或电脑 IP 不在 192.168.1.XXX 网段 |
| 雷达有数据但动态点云是乱的 | PCD 地图和新场景不匹配，需要重新录制 |
| launch 启动报错找不到插件 | camera_node 占位未实现，先删掉再启动 |
| 相机无数据 | 话题名不是 `camera_image`，或消息类型不是 `sensor_msgs/Image` |
| rosbag 测试正常但真实硬件不工作 | 确认各话题名和消息类型与代码中的一致 |
| rviz2 看不到点云 | Fixed Frame 下拉框打不出 `livox_frame`？驱动不发 TF，手动输入即可 |
| `radar.launch.py` 启动报错找不到插件 | `databag_tool::BagRecorderNode` 不存在，在 launch 文件中注释掉（见 5.1） |
| `foxglove_bridge` 找不到 | `sudo apt install ros-humble-foxglove-bridge`，或在 launch 中注释掉 |
